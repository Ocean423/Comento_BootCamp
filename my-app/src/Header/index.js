import React from 'react';
import {HeaderContainer, HeaderTitle} from "./styled";

const Header = () => {
    return(
        <HeaderContainer>
            <HeaderTitle>코멘토 1차 과제</HeaderTitle>
        </HeaderContainer>

    );
};

export default Header;